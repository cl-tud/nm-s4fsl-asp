% S4F standpoint logic minimal models encoding

% declare theory via atoms form/1
% containing formulas using:
% atoms: p, q, r, ...
% composites: neg(F), and(F, G), box(S, F)

% optionally declare standpoint names via sn/1
% special standpoint name * represented by "all"

%% show only final partition

#show known/1.
% #show unknown/1.